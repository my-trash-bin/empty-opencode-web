{
	admin off
	auto_https off
	# IP-address clients may omit SNI during the TLS handshake.
	default_sni localhost
}

https://localhost, https://127.0.0.1 {
	tls /certs/localhost.pem /certs/localhost-key.pem
	reverse_proxy opencode:4096 {
		flush_interval -1
	}
}
